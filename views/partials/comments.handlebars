<div class=" row comment-container">
    {{#each this}}
    <section class="twelve columns comment-container">
        <div class="meta">
            {{user.username}} on {{created_at}}
        </div>
        <div class="text">
            {{comment_text}}
        </div>
    </section>
    {{/each}}
</div>